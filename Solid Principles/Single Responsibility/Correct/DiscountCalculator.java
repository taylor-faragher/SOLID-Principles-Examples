public class DiscountCalculator {
    public void calculateDiscount(AsurionCustomer customer) {
        if (customer.getInsuranceType().equals("regular")) {
            //calculate discount
        }
        if (customer.getInsuranceType().equals("vip")) {
            //calculate discount
        }
    }
}